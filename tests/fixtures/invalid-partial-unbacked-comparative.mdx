---
title: "Partial Unbacked Comparative Frequency Test"
description: "Demonstrates rejection when one comparative assertion has backing but another in the same document does not."
schemaVersion: "1.0"
contentKind: "explainer"
readerIntent: "understand"
readerOutcome: "Understand that each comparative frequency statement requires its own empirical claim."
thesis: "One backed comparative frequency statement does not authorize additional unbacked comparative claims."
shortAnswer: "A policy-governed runtime moves authorization out of the prompt and into a deterministic host supervisor. The model proposes tool calls, but the host environment verifies permissions against immutable rules before execution begins."
boundary:
  is: "A test fixture for occurrence-bound comparative frequency."
  isNot: "A published explainer."
  mattersWhen: "Testing comparative frequency gates."
provenance:
  primary: "external"
  sources:
    - id: "benchmark-study"
      type: "benchmark"
      url: "https://example.com/concurrency-benchmarks-2025"
  claims:
    - statement: "The optimistic locking algorithm fails more often than pessimistic locking under contention."
      kind: "external"
      sources:
        - "benchmark-study"
---

The optimistic locking algorithm fails more often than pessimistic locking under contention.

In high concurrency networks, UDP transmission fails more often than TCP connections without retry.
