---
title: "Prohibited Vale Prose Pattern Test"
description: "Demonstrates rejection when valid v1 structure contains a prohibited Vale prose pattern."
schemaVersion: "1.0"
contentKind: "explainer"
readerIntent: "understand"
readerOutcome: "Understand that Vale prose rules block publication of v1 content."
thesis: "Vale prose rules must actively block invalid drafts in WebsiteOps."
shortAnswer: "A policy-governed runtime moves authorization out of the prompt and into a deterministic host supervisor. The model proposes tool calls, but the host environment verifies permissions against immutable rules before execution begins."
boundary:
  is: "A test fixture for Vale blocking gate."
  isNot: "A published explainer."
  mattersWhen: "Testing WebsiteOps prose gates."
provenance:
  primary: "synthesis"
  claims:
    - statement: "Vale prose rules must actively block invalid drafts in WebsiteOps."
      kind: "synthesis"
---

In this article we will examine how model tool execution requires a host-level boundary.

Vale prose rules must actively block invalid drafts in WebsiteOps.
